---
// Hero Component - 3D Moon Variant
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-cyber-darker" id="home">
  <!-- 3D Moon Animation -->
  <div class="absolute inset-0 flex items-center justify-center">
    <div class="moon-container">
      <div class="moon-sphere"></div>
    </div>
  </div>

  <!-- Subtle Grid Background -->
  <div class="absolute inset-0 cyber-grid opacity-10"></div>

  <!-- Content - Clean and Spacious -->
  <div class="relative z-10 text-center px-4 max-w-4xl mx-auto">
    <div class="mb-8 md:mb-16">
      <h1 class="text-4xl sm:text-5xl md:text-7xl lg:text-8xl font-display text-neon-cyan mb-4 md:mb-6 tracking-tight">
        ATILA LIMA
      </h1>
      <div class="h-px w-24 md:w-32 mx-auto bg-gradient-to-r from-transparent via-neon-cyan to-transparent mb-6 md:mb-8"></div>
    </div>

    <p class="text-base sm:text-lg md:text-xl lg:text-2xl text-gray-400 mb-10 md:mb-16 font-mono max-w-2xl mx-auto leading-relaxed px-4">
      Software Engineer <span class="text-neon-purple">•</span> Full-Stack Developer
    </p>

    <div class="flex flex-col sm:flex-row gap-4 sm:gap-6 justify-center items-center px-4">
      <a href="#about" class="neon-button w-full sm:w-auto px-8 md:px-10 py-3 md:py-4 font-mono text-sm md:text-base">
        View Work
      </a>
      <a href="#contact" class="w-full sm:w-auto px-8 md:px-10 py-3 md:py-4 border border-gray-700 text-gray-400 hover:border-neon-cyan hover:text-neon-cyan transition-all font-mono rounded text-sm md:text-base">
        Get In Touch
      </a>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-12 left-1/2 transform -translate-x-1/2">
    <div class="w-6 h-10 border border-neon-cyan/30 rounded-full flex justify-center p-2">
      <div class="w-1 h-3 bg-neon-cyan/50 rounded-full animate-bounce"></div>
    </div>
  </div>
</section>

<style>
  /* 3D Moon Animation */
  .moon-container {
    perspective: 1000px;
    width: 100%;
    max-width: 400px;
    height: 400px;
    position: relative;
  }

  .moon-sphere {
    width: 100%;
    height: 100%;
    position: relative;
    transform-style: preserve-3d;
    animation: rotateMoon 30s linear infinite;
  }

  .moon-sphere::before,
  .moon-sphere::after {
    content: '';
    position: absolute;
    border-radius: 50%;
  }

  /* Main moon body with wireframe effect */
  .moon-sphere::before {
    inset: 0;
    background: radial-gradient(circle at 30% 30%,
      rgba(0, 176, 0, 0.15) 0%,
      rgba(0, 176, 0, 0.08) 40%,
      transparent 70%);
    border: 2px solid rgba(0, 176, 0, 0.3);
    box-shadow:
      inset 0 0 60px rgba(0, 176, 0, 0.1),
      0 0 80px rgba(0, 176, 0, 0.15),
      0 0 120px rgba(0, 176, 0, 0.08);
  }

  /* Glow effect */
  .moon-sphere::after {
    inset: -20px;
    background: radial-gradient(circle,
      rgba(0, 176, 0, 0.1) 0%,
      transparent 60%);
    filter: blur(30px);
  }

  /* Latitude lines */
  .moon-sphere {
    background-image:
      repeating-linear-gradient(0deg,
        transparent,
        transparent 38px,
        rgba(0, 176, 0, 0.15) 38px,
        rgba(0, 176, 0, 0.15) 40px
      );
    border-radius: 50%;
  }

  @keyframes rotateMoon {
    0% {
      transform: rotateY(0deg) rotateX(10deg);
    }
    100% {
      transform: rotateY(360deg) rotateX(10deg);
    }
  }

  /* Make moon responsive */
  @media (max-width: 768px) {
    .moon-container {
      max-width: 280px;
      height: 280px;
    }
  }

  @media (max-width: 480px) {
    .moon-container {
      max-width: 220px;
      height: 220px;
    }
  }

  @media (max-width: 360px) {
    .moon-container {
      max-width: 180px;
      height: 180px;
    }
  }
</style>
